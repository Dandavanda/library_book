<%= form_with(model: return, local: true) do |form| %>
  <% if return.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(return.errors.count, "error") %> prohibited this return from being saved:</h2>

      <ul>
      <% return.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date_borrower %>
    <%= form.date_select :date_borrower %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
